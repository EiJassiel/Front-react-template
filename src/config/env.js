const env = {
  appName: import.meta.env.VITE_APP_NAME || 'Front React Template',
  appDescription:
    import.meta.env.VITE_APP_DESCRIPTION ||
    'Plantilla base para proyectos frontend con React, Vite y JSX.',
  docsUrl: import.meta.env.VITE_DOCS_URL || 'https://react.dev/',
}

export default env
